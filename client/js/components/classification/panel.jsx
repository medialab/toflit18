/**
 * TOFLIT18 Client Classification View
 * =====================================
 *
 * Displayed the view used to aggregate data.
 */
import React from 'react';

export default function ClassificationPanel({children}) {
  return <div id="classification">{children}</div>;
}
